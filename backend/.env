PORT=5000
MONGO_URI=mongodb://localhost:27017/dot-jordan
JWT_SECRET=local_dev_only_secret_do_not_use_in_production_0a1b2c3d4e5f
JWT_EXPIRES_IN=7d
CLIENT_ORIGIN=http://localhost:3000
PUBLIC_API_URL=http://localhost:5000
DEMO_STUDENT_PASSWORD=DotStudent2026!
DEMO_TEACHER_PASSWORD=DotTeacher2026!
DEMO_ADMIN_PASSWORD=DotAdmin2026!
DEMO_SUPERADMIN_PASSWORD=DotSuper2026!

# Stripe payments — set your TEST keys here to enable /api/payments
# (leave blank to keep payments disabled; endpoints then return 503).
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PUBLISHABLE_KEY=

# SMTP email — set these to enable real email delivery. Leave blank to keep
# emails disabled (the app logs email content to the console instead).
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

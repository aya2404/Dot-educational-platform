# FRONTEND_FIXES.md — Dot Jordan

All fixes preserve the existing visual identity and business behavior. No libraries added. No functionality removed. Verified against the running app (production build served on :3000) and/or the production build output.

| ID | Severity | Problem | Root Cause | Fix | File(s) | Verification | Status |
|----|----------|---------|-----------|-----|---------|--------------|--------|
| FE-1 | P2 | Login screen used a **different** primary-button style and double-spaced inputs vs the rest of the app; the override was an unscoped global selector that could leak app-wide. | `LoginPage.css` declared unscoped `.btn-primary` (flat `#6d5acf`, radius 40px) and `.login-card input` (own radius + `margin-bottom:20px`), fighting the design system and adding margin on top of the form's `gap-3`. | Removed the page-level overrides so the login button/inputs inherit the shared `.btn-primary` / `.form-control` styling. | `frontend/src/pages/LoginPage.css` | Reloaded `/login`: submit button computes the app gradient; inputs `margin-bottom:0` (even spacing); screenshot shows clean, consistent card; **real form login → `/student`** works. | ✅ Verified |
| FE-2 | P2 (a11y) | Form labels weren't programmatically tied to inputs — clicking a label didn't focus it and screen readers couldn't name the field. | `<label className="form-label">` had no `htmlFor`; inputs/selects had no `id`. 5 fields (create-user, enrollment) + 7 (create-content). | Added matching `htmlFor`/`id` to every single-control field. | `frontend/src/pages/SuperAdminDashboard.jsx`, `frontend/src/pages/CreateContentPage.jsx` | Probe on both pages: **every** single-control label's `htmlFor` resolves to an existing element (`allResolve:true`), incl. conditional task fields (`content-due-date`, `content-max-score`). | ✅ Verified |
| FE-3 | P2 (a11y) | Modals could not be closed with the keyboard and weren't announced as dialogs. | `ConfirmModal` & `TaskSubmissionModal` had no `Escape` handler and no `role="dialog"`/`aria-modal`. | Added a document `keydown` → Escape handler (respecting `loading`) with cleanup, plus `role="dialog"`, `aria-modal="true"`, `aria-label`. | `frontend/src/components/common/ConfirmModal.jsx`, `frontend/src/components/student/TaskSubmissionModal.jsx` | Opened the delete `ConfirmModal` in-browser (attrs present: `role=dialog`, `aria-modal=true`, `aria-label="حذف المحتوى"`); pressed **Escape** → modal removed and `body.overflow` restored. TaskSubmissionModal uses the identical pattern. | ✅ Verified |
| FE-4 | P2 (a11y) | The modal's close (X) button had no accessible name. | Icon-only `<button><BsXLg/></button>` in `TaskSubmissionModal`. | Added `aria-label="إغلاق"`. | `frontend/src/components/student/TaskSubmissionModal.jsx` | Present in the production bundle (`aria-label` grep) and in source. | ✅ Verified (build) |
| FE-5 | P3 (perf) | Font-loading mismatch: `index.html` loaded IBM Plex + an **unused Tajawal**; `global.css` separately `@import`ed Cairo + IBM Plex + Manrope (render-blocking, and IBM Plex double-loaded). | Fonts split across two mechanisms with a dead family. | Consolidated **all** fonts into the single preconnected `<link>` in `index.html` (Cairo + IBM Plex + Manrope), dropped Tajawal, and **removed the render-blocking `@import`** from `global.css`. | `frontend/public/index.html`, `frontend/src/styles/global.css` | In-browser: the link resolves with Cairo+IBMPlex+Manrope, **Tajawal gone**, `document.fonts.check('Cairo')` = true (headings render), fonts URL returns 200, no console/network errors; build compiles. | ✅ Verified |
| FE-7 | P3 (CSS) | **Duplicate timeline CSS**: `.timeline-view/.timeline-entry/...` were defined in **both** `global.css` and `Timeline.css`. Because `global.css` loads later, it overrode `Timeline.css`'s intended `padding`/`gap` while `Timeline.css` still supplied the dots/line — a load-order "Frankenstein". | Same selectors duplicated across two stylesheets. | Removed the timeline block (and its tablet `.timeline-entry` override) from `global.css`, making `Timeline.css` the single source of truth. | `frontend/src/styles/global.css` | In-browser: `.timeline-entry` padding now `22px…` and `.timeline-view` gap `26px` (Timeline.css intended values), dots present; **re-tested timeline for overflow at 320/768/1280 → none**; 10 entries + 9 date groups render. | ✅ Verified |
| FE-11 | P2 (a11y) | The login page started its heading hierarchy at `<h2>` with **no `<h1>`** — a screen-reader page-title gap (dashboards already use h1). | The login card title was `<h2 class="login-card__title">`. | Changed it to `<h1>` (styling is class-based, so the visual is unchanged). | `frontend/src/pages/LoginPage.jsx` | In-browser a11y probe on `/login` (via rebuilt bundle): the page now exposes an H1; screenshot shows identical visual. | ✅ Verified |
| FE-10 | P2 (a11y) | Modals had Escape + backdrop close + dialog role but **no focus trap** — keyboard focus could leave the open modal and land on the page behind it. | No focus management on open/close. | Added a small reusable `useFocusTrap(active)` hook: on open it moves focus into the dialog, keeps `Tab`/`Shift+Tab` cycling inside it, and restores focus to the previously-focused element on close. Wired into both modals (`ref` + `tabIndex={-1}`). | `frontend/src/hooks/useFocusTrap.js` (new), `frontend/src/components/common/ConfirmModal.jsx`, `frontend/src/components/student/TaskSubmissionModal.jsx` | In-browser, **fully verified** on both ConfirmModal consumers (Timeline delete-content + nested delete-submission) and TaskSubmissionModal: focus moves **into** the dialog on open; `Tab` at last **wraps to first**; **Shift+Tab** at first **wraps to last**; Escape **and** Cancel close it and **restore focus to the trigger**; disabled buttons excluded while `loading`; **no trap remains after close** (can focus outside freely). | ✅ Verified |
| FE-13 | P2 (UX) | With the nested delete-submission `ConfirmModal` open inside `TaskSubmissionModal`, a single **Escape closed BOTH** dialogs (both registered document-level Escape handlers from FE-3). | Two modals each listening for Escape on `document`. | Guarded `TaskSubmissionModal`'s Escape handler with `!showDeleteConfirm` (and added it to the effect deps) so it ignores Escape while its nested confirm is open — Escape now closes only the **topmost** dialog. | `frontend/src/components/student/TaskSubmissionModal.jsx` | In-browser: with the nested confirm open, **1st Escape closes only the confirm** (task modal stays open, focus returns to it); **2nd Escape closes the task modal**. | ✅ Verified |

## Intentionally NOT changed (documented in FRONTEND_AUDIT_REPORT.md)
| ID | Why not changed |
|----|-----------------|
| FE-6 | Bootstrap import order overrides the design-system organic radii **and the body font** (`body` renders in the system Arabic font, not IBM Plex) **app-wide**, but the result is currently *visually consistent*. Forcing the design tokens to win would restyle every page's buttons/inputs/body type — an unrequested redesign; left as a deliberate design decision. |
| FE-8 | Inline layout style hacks in `TeacherDashboard`/`CoursePage` — functional; not worth regression risk. |
| FE-9 | 3 create-content labels are group/section headings (button group, uploader, external-link pair), not single controls; inputs carry placeholders. |
| FE-12 | Timeline heading order skips a level (course `h1` → item titles `h3`); changing the tag would alter the visual hierarchy — documented. |

## Files changed (whole frontend audit — 9 source files: 8 modified + 1 new)
```
 frontend/public/index.html                              (font consolidation: +Cairo/Manrope in <link>, -Tajawal)
 frontend/src/styles/global.css                          (-render-blocking @import; -duplicate timeline block)
 frontend/src/pages/LoginPage.jsx                         (h2 → h1 title)
 frontend/src/pages/LoginPage.css                         (-unscoped global overrides)
 frontend/src/pages/SuperAdminDashboard.jsx               (+label htmlFor/id ×5)
 frontend/src/pages/CreateContentPage.jsx                 (+label htmlFor/id ×7)
 frontend/src/components/common/ConfirmModal.jsx          (+Escape + dialog semantics + focus trap)
 frontend/src/components/student/TaskSubmissionModal.jsx  (+Escape + dialog + close aria-label + focus trap + FE-13 nested-Escape guard)
 frontend/src/hooks/useFocusTrap.js                       (NEW — reusable modal focus-trap hook)
```

## Build & regression after fixes (final, post FE-13)
- `CI=true npm run build` → **Compiled successfully, 0 lint warnings** (with the focus-trap hook + FE-13).
- **Responsive (FINAL explicit 9-width matrix, re-run this turn):** every real route verified via same-origin iframe viewports (media queries fire at the iframe width; confirmed by the hamburger/off-canvas toggle at 992px) — **zero horizontal overflow at every measured width**:
  - **timeline @ all 9** (375,390,414,768,820,1024,1280,1440,1920) · **login @ all 9** · **student @ all 9** (measured) · **teacher @ all 9** (measured, table) · **create-content @ all 9** · **superadmin+Courses @ 6** (414/1024/1920 dropped by tab-click throttle; within already-clean regimes) · **admin @ 375/768/1280** (prior; `mode="admin"`, layout-identical to superadmin) · **TaskSubmissionModal @390**.
  - Sidebar → off-canvas drawer + hamburger below 992px, fixed rail ≥992px; wide tables scroll in-card. (Widths within one breakpoint regime — mobile <576 / tablet 576–991 / desktop ≥992 — render identically; the timeline's full 9-width sweep confirms fluid scaling holds within regimes.)
- **Focus trap (FE-10) — fully verified:** focus-into-dialog on open, `Tab`/`Shift+Tab` wrap inside, Escape+Cancel close + **restore focus to trigger**, disabled buttons excluded while loading, no trap after close; verified on both ConfirmModal consumers + TaskSubmissionModal + the nested case.
- **Nested-modal Escape (FE-13) — verified:** Escape closes only the topmost dialog.
- **A11y:** 0 images without alt, 0 unnamed buttons (of 26 on the course page), all form labels resolve, single H1, `tabIndex=-1` only on modal containers (0 on interactive elements), modals `role=dialog`+`aria-modal`+focus-trap.
- **Fonts:** consolidated `<link>` loads Cairo/IBMPlex/Manrope (Tajawal removed), Cairo renders, fonts URL 200, no 404s.
- **Console/network:** **no app console output at all** on fresh loads (no errors, no React warnings); no failed requests. (Any `getComputedStyle` errors in the console are from the audit's own probe scripts, not the app.)
- Backend unaffected: `npm test` **5/5**, API smoke **20/20 + 10/10**, services up (backend 200, frontend 200). Dev DB pristine after tests (temp task/submission/admin used for testing were deleted; users=21, contents=10, submissions=0).
